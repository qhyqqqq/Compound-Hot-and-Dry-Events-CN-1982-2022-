{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: serve",
            "request": "launch",
            "command": "npm run serve",
            "cwd": "${workspaceFolder}\\highTemp"
        }
    ]
}